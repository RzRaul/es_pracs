#include <stdint.h>
#include <stdio.h>
#include <string.h>
int decode_compression(char *zip) {}
int process_compression(char *zip, int len) {
  int aux_bytes = 0;

  for (uint8_t i = 0; i < len; i++) {
    if (zip[i] == '[') {
    }
  }
}

void app_main(void) {}
